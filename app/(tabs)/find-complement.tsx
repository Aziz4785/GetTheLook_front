import { Text, View } from 'react-native';

export default function FindComplementScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Find a Complement Page</Text>
    </View>
  );
}